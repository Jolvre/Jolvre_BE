spring.application.name=Graduate_Minions
spring.profiles.include=oauth,swagger,email,redis
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL}
#jdbc:postgresql://localhost:5432/jolvre
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
#db - connection - Maria
#spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
#spring.datasource.url=${dev-Db-Url}
#spring.datasource.username=${dev-Db-Id}
#spring.datasource.password=${dev-db-Pw}
#db - connection - Postgresql
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.url=jdbc:postgresql://localhost:5432/jolvre
#spring.datasource.username=gosu
#JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.generate-ddl=true
#jwt
jwt.secretKey=${JWT_SECRET_KEY}
jwt.access.expiration=3600000
jwt.access.header=Authorization
jwt.refresh.expiration=1209600000
jwt.refresh.header=Authorization-refresh
#verifyStudent
verify.student.apiKey=${VERIFY_STUDENT_APIKEY}

